<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pet.mapper.UserMapper">
    <insert id="insert" parameterType="com.pet.entity.User">
        INSERT INTO pet.user (user_id, username, phone_number, password, sex, create_time, update_time)
        VALUE (#{userId},#{username},#{phoneNumber}, #{password}, #{sex}, #{createTime}, #{updateTime})
    </insert>
</mapper>