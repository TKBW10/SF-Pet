<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pet.mapper.UserHomeMapper">
    <insert id="addRoom" parameterType="com.pet.entity.UserHome">
        INSERT INTO user_home (user_id, room_id, room_name, create_time, update_time)
            VALUE (#{userId}, #{roomId}, #{roomName}, #{createTime}, #{updateTime})
    </insert>
</mapper>